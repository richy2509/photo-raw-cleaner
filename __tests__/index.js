describe('Photo cleaner', () => {

    beforeAll(() => {

        //let i = command.parse(process.argv);
        //console.log(i.destination);

    });

    it('should have destination well formatted', () => {

      console.log("bonjour");
      //const timerGame = require('../index');
        //timerGame();

        //expect(setTimeout.mock.calls.length).toBe(1);
        //expect(setTimeout.mock.calls[0][1]).toBe(1000);


        //expect(params.destination.value).equals("bonjour");

    });

});